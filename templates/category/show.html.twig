<!doctype html>

<html lang="en">

	<head>

		<meta charset="UTF-8">

		<title>Wild Series</title>

	</head>

	<body>

		<h1>Toutes les séries de la catégorie {{category.name}} :
		</h1>

		{% for program in programs %}

			<div>

				<h2>{{ program.title }}
					-
					ID: {{ program.id }}</h2>

				<p>{{ program.summary }}</p>

				{# Lien vers la fiche de la série : #}

				<a href="/programs/{{program.id}}">Voir la fiche</a>

			</div>

		{% else %}

			Aucune série trouvée.

		{% endfor %}


		<a href="{{ path('app_index') }}">

			Retour à l'accueil

		</a>

	</body>

</html>
